# 问道志阶段执行状态

## 当前版本

V1.4 第二批方法与图谱构建准备阶段

## 当前目标

七个核心概念、六部核心经典、六个高连接人物、五个流派/传统与五个方法节点已经完成 V3 迁移。当前重点转向：

1. 迁移导引、吐纳、服气、静坐等第二批方法；
2. 建立《抱朴子》与天师道等当前 planned 节点；
3. 完成全库正式节点、计划节点、关系端点与来源 ID 的自动检查基础；
4. 开始生成 `nodes.json / relations.json / graph.json` 的第一版构建数据。

仍以 Git 仓库实际内容为唯一进度依据。

## 已完成

### 一、项目规范与数据模型

- 知识节点、证据、来源、知识卡编写规范；
- 六类知识卡模板；
- 知识卡数据模型；
- 节点关系语义规范；
- `traditional_attribution`：传统归属关系；
- `founded`：有历史证据的宗教组织/运动创建关系；
- `school_kind`：思想传统、宗教宗派、宗教传统、修炼传统等细分类；
- 来源标识注册表 V1.3；
- 知识图谱 V3.4 清单式主入口；
- 首轮数据一致性人工检查。

### 二、核心概念

实际 Git 完成度：**7 / 7**。

- `concept.dao` 道；
- `concept.qi` 气；
- `concept.jing` 静；
- `concept.xu` 虚；
- `concept.xin` 心；
- `concept.xi` 息；
- `concept.shen` 身。

### 三、第一批经典

实际 Git 完成度：**6 / 6**。

- `classic.daodejing` 《道德经》；
- `classic.zhuangzi` 《庄子》；
- `classic.zhouyi` 《周易》；
- `classic.huangdineijing` 《黄帝内经》；
- `classic.cantongqi` 《周易参同契》；
- `classic.wuzhenpian` 《悟真篇》。

### 四、高连接人物

实际 Git 完成度：**6 / 6**。

- `person.laozi` 老子；
- `person.zhuangzi` 庄子；
- `person.zhangboduan` 张伯端；
- `person.gehong` 葛洪；
- `person.weiboyang` 魏伯阳；
- `person.wangchongyang` 王重阳。

第二批重要修正：

- 葛洪按《抱朴子》、早期炼丹与养生史处理，不放入后世内丹直传链；
- 魏伯阳标记为历史性不确定的传统人物；
- 魏伯阳—《参同契》只使用 `traditional_attribution`；
- 王重阳—全真道使用 `founded`；
- “五祖七真”按后世谱系建构层处理，不反推为王重阳生前已固定。

### 五、流派与传统分类

实际 Git 完成度：**5 / 5**。

| 节点 | `school_kind` |
| --- | --- |
| `school.xianqin_daojia` 先秦道家 | `intellectual_tradition` |
| `school.huanglao` 黄老思想 | `intellectual_tradition` |
| `school.danding` 丹道传统 | `practice_tradition` |
| `school.quanzhen` 全真道 | `religious_order` |
| `school.zhengyi` 正一道 | `religious_tradition` |

已完成关键修正：

- “道家”作为后起思想史分类，不设置老子为创建者；
- 黄老与先秦道家相关但不等同；
- “丹鼎派”保留兼容名，正式建模改为“丹道传统”；
- 全真道不等于内丹；
- 正一道不等于符箓；
- 全真/正一不使用“出家内丹 vs 在家符箓”的绝对二分。

详细见：`32-争议辨析层/流派分类与丹鼎概念辨析.md`。

### 六、方法节点

当前正式 V3 完成：**5 个**。

- `method.neidan` 内丹；
- `method.waidan` 外丹；
- `method.xinzhai` 心斋；
- `method.zuowang` 坐忘；
- `method.fulu_keyi` 符箓科仪。

其中符箓科仪已明确为宗教仪式研究节点，不与静坐、导引等个人修炼方法放在同一实践步骤链中。

### 七、规范关系数据

已建立：

- `31-知识图谱整合/核心概念关系-v1.yml`；
- `31-知识图谱整合/经典关系-v1.yml`；
- `31-知识图谱整合/人物与方法关系-v1.yml`；
- `31-知识图谱整合/人物与流派关系-v1.yml`。

新增关系原则：

- `traditional_attribution` 用于魏伯阳—《参同契》等传统归属；
- `founded` 只用于王重阳—全真道等较明确历史创建关系；
- `intellectual_tradition` 不自动拥有创建者；
- `practice_tradition` 不伪装成统一教团。

## 当前正式 V3 高连接节点

```text
核心概念 7
经典     6
人物     6
流派/传统 5
方法     5
──────────
合计     29 个正式节点
```

## 当前 planned 节点

V3.4 已显式登记：

- `classic.baopuzi` 《抱朴子》；
- `school.tianshi` 天师道；
- `method.daoyin` 导引；
- `method.tuna` 吐纳；
- `method.fuqi` 服气；
- `research.medobs` 身体观察研究。

## 当前主要数据债务

### 1. 第二批方法仍是旧结构

下一批优先：

1. 导引；
2. 吐纳；
3. 服气；
4. 静坐。

重点避免：

- 导引 = 现代健身；
- 吐纳 = 调息 = 息；
- 服气 = 单纯深呼吸；
- 静坐 = 坐忘；
- 后世方法术语无说明地读回先秦经典。

### 2. planned 经典与宗教传统

- 《抱朴子》需要拆分内篇/外篇与成书、文本问题；
- 天师道需要与后世正一道分层；
- 张道陵人物历史与传统传记需另做审计。

### 3. 自动构建仍未实现

目前 V3 已具备：

- 正式节点清单；
- planned 节点清单；
- 关系 YAML；
- 来源注册表；
- 数据质量门。

下一步需要真正生成：

```text
data/nodes.json
data/relations.json
data/graph.json
```

并建立最小自动检查脚本。

## 下一批执行顺序

### 第一优先级：第二批方法

导引 → 吐纳 → 服气 → 静坐。

### 第二优先级：自动检查与构建

- ID 唯一；
- `school_kind` 完整；
- relation 端点；
- source_refs；
- 对称关系去重；
- planned 与正式节点区分；
- 生成第一版 JSON 图谱数据。

### 第三优先级：补关键 planned 节点

《抱朴子》、天师道、张道陵。

## 执行原则

每轮迭代必须进行：

1. 读取仓库实际状态；
2. 读取旧卡和已有专题证据；
3. 新版知识卡迁移；
4. 作者、年代、师承和影响关系单独核对；
5. 更新机器可读关系；
6. 做来源和端点一致性检查；
7. Git 提交；
8. 可量化进度变化时同步本状态文件。
