# 问道志浏览器与线上部署验证 V1.3

更新时间：2026-08-13

## 一、验证分层

问道志网站由 GitHub main 更新后自动部署到：

`https://wendaozhi.pages.dev/`

当前质量链：

```text
规则 / 数据测试
↓
实践工作台 Chromium E2E
↓
今日修持与双向导航 Chromium E2E
↓
日读 / 夜读 Chromium E2E
↓
静态构建产物检查
↓
Cloudflare Pages 当前提交线上验证
```

单元测试回答“规则是否正确”；真实 Chromium 回答“用户实际操作是否成立”；生产 smoke 回答“线上是否已经部署当前提交并保留关键入口”。

## 二、通用实践工作台 Chromium

脚本：`website/scripts/e2e-practice-browser.mjs`

命令：

```text
npm run test:e2e:practice
```

当前覆盖：

1. `/practice/` 实际加载；
2. `tablist/tab` 键盘语义；
3. 调身按最小字段保存；
4. 隐藏字段不会污染记录；
5. localStorage 写入与刷新保留；
6. “今天决定不练”归零且不计失败；
7. JSON 导出与真实文件导入；
8. 删除/清空取消与确认；
9. 剪贴板拒绝后的手动复制降级；
10. 记录不足时不自动推进阶段；
11. 红色事件后阶段判断暂停；
12. 390px 窄屏无明显横向溢出。

Chromium DevTools 启动等待已加固为更长等待窗口，并在浏览器提前退出时给出明确诊断，避免 CI 偶发启动抖动被误判为产品失败。

## 三、今日修持与双向导航 Chromium

脚本：`website/scripts/e2e-practice-daily.mjs`

命令：

```text
npm run test:e2e:daily
```

### V2.5 日常减负继续验证

- 第一次从“准备与安全检查”开始；
- 今日入口显示阶段方向；
- 自然察息只显示呼吸 + 练后状态；
- 短时基础安坐显示姿势、呼吸、注意、练后四类观察；
- 保存“调身”后只显示“继续上次：调身与舒适姿势”；
- 不自动推进到下一实践卡；
- “今天决定不练”进入最简表单。

### V2.6 双向实践导航新增验证

真实浏览器完整执行：

```text
进入 /practice/card/natural-breath
↓
找到“记录本次实践”
↓
点击进入 /practice/?practice=natural-breath#practice-journal
↓
工作台预选“自然察息”
```

随后断言：

- 进入前后 localStorage 记录数不增加；
- 页面没有自动保存；
- 预选后仍只显示自然察息需要的最小字段；
- 查询参数不会改变阶段。

### 安全状态覆盖 URL 参数

测试会在当前本地日期写入一条红色记录，然后刷新仍带：

```text
?practice=natural-breath
```

的工作台页面。

预期必须是：

```text
practice.basic.precheck
```

而不是自然察息，并显示：

> 实践卡链接不能绕过安全状态。

这保证便利导航永远低于安全规则。

## 四、阅读主题 Chromium

脚本：`website/scripts/e2e-theme-browser.mjs`

命令：

```text
npm run test:e2e:theme
```

验证：

- 日读 / 夜读双向切换；
- 主要纸色与墨色计算样式真实变化；
- `aria-pressed` 与按钮提示同步；
- `wendaozhi.reading.theme` 持久化；
- 刷新恢复；
- 390px 移动端入口。

## 五、31 个规则与数据场景

`npm run test:practice` 当前运行 31 个实践/schema 场景，覆盖：

- 数据规范化；
- 每张实践卡最小字段；
- 不练记录；
- 7天统计；
- 30天阶段判断；
- 黄色/红色安全规则；
- AI 摘要边界；
- schema v1 与未来版本迁移保护。

## 六、practice:check 的 V2.6 硬约束

`npm run practice:check` 现在除原有实践结构检查外，还必须确认：

- `PracticeJournal` 存在 `applyPracticeEntryFromUrl`；
- 使用 `URLSearchParams` 读取实践卡入口；
- 工作台保留 `practice-journal` 锚点；
- URL 入口使用已载入记录重新计算安全状态；
- 实践页面生成器包含“记录本次实践”；
- 生成链接包含 `?practice=` 与 `#practice-journal`；
- 今日修持 E2E 覆盖“双向实践卡导航”；
- E2E 覆盖“实践卡链接不能绕过安全状态”。

因此双向导航不是一次性 UI 文案，而是持续受质量门保护的产品约束。

## 七、Cloudflare Pages 当前提交验证

脚本：`website/scripts/smoke-production.mjs`

main push 后：

```text
读取 github.sha
↓
轮询线上 /build-info.json
↓
等待 Pages commit 追上当前提交
↓
检查线上关键页面与版本锚点
```

默认：

```text
站点：https://wendaozhi.pages.dev
最多检查：36次
间隔：10秒
```

### 实践工作台锚点

```text
PRACTICE-004
今日修持
30天与阶段
今天决定不练
practice-journal
```

### V2.6 实践卡锚点

线上 `/practice/card/natural-breath` 必须包含：

```text
记录本次实践
practice=natural-breath
practice-journal
```

仍然继续验证 `/library/` 重定向和 `.html` → 无扩展名规范实践卡 URL。

## 八、CI 并发

```text
concurrency:
  group: website-${{ github.ref }}
  cancel-in-progress: true
```

连续提交时，只让最新提交继续等待 Cloudflare 当前 SHA，避免旧提交验证一个不会再成为线上最新版本的构建。

## 九、自动化曾暴露的问题

当前浏览器/生产链已经真实发现并修正过：

1. Cloudflare `.html` URL 会 308 规范化，不能按本地文件名验证；
2. 页面版本变化后，旧 smoke 文案锚点会误报；
3. Chromium DevTools 偶发启动超过原等待窗口，需要更稳的启动诊断；
4. 同一个 URL 再次赋给 `location.href` 不等同于重新挂载页面，安全覆盖测试必须用真实刷新；
5. URL 入口安全决策不能依赖响应式计算属性刷新时机，应直接根据刚载入记录计算。

这些问题没有通过删除检查来解决，而是修正测试或产品实现，使验证更接近真实行为。

## 十、仍需真机 / 长期验证

- Edge；
- Safari；
- iOS Safari；
- Android Chrome；
- 手机真实文件选择和下载；
- 不同屏幕夜读视觉；
- 屏幕阅读器；
- 连续真实7天/30天后的信息密度与字段价值；
- 双向导航在真实日常使用中的操作负担；
- 用户是否误解“阶段方向/记录支持稳定”；
- 真实未来 schema v2 数据迁移。

## 十一、结论

当前工程链已经达到：

```text
规则与数据自动测试
+ 通用实践 Chromium
+ 今日修持 / 双向导航 Chromium
+ 阅读主题 Chromium
+ Cloudflare 当前提交线上验证
```

自动化证明的是产品行为可重复、边界不容易回归、线上版本可追踪；不证明传统修炼方法本身的效果。

八张基础实践卡继续保持：

`status: review`
