# 当前聚焦工作包

当前工作包：PRACTICE-003「30天长期复盘、阶段判断与记录迁移保护」
当前状态：V2.3 自动化基线完成，进入真实浏览器试跑
当前基线：V3.8 知识图谱稳定；V2.0 基础实践 8 张；V2.3 已具备7天复盘、30天阶段复盘、知识回链和 schema 迁移保护
当前分支：main

## 当前状态四句话

- **知识层保持稳定：** V3.8 继续维持 38 个正式节点、79 条规范关系，现代实践顺序仍不进入历史图谱。
- **长期复盘已落地：** 实践工具现在同时提供每日记录、最近7天复盘、30天分布、四类基础能力状态与阶段方向。
- **阶段判断不等于晋级：** 系统只输出“记录支持稳定 / 部分稳定 / 尚未稳定 / 记录不足 / 因安全原因暂停”，即使数据充分也只允许“讨论分流”。
- **数据升级已有保护：** 当前仍是 schema v1，但本地读取和 JSON 导入都已接入显式迁移器；未来版本不能被旧程序自动降级或覆盖。

---

## V2.3 已完成

### 1. 六个独立规则模块

当前实践工具核心逻辑已拆成：

```text
practice-model.mjs       数据模型 / 草稿校验 / 导入导出
practice-migrations.mjs  schema 版本识别 / 显式迁移 / 未来版本保护
practice-stats.mjs       7天/30天窗口与聚合统计
practice-safety.mjs      安全规则与填写期提醒
practice-ai.mjs          7天/30天 AI 摘要边界
practice-stage.mjs       30天分布 / 四类能力 / 阶段方向
```

`PracticeJournal.vue` 只负责界面、本地存储调用和用户交互，不再独自持有业务规则。

### 2. 30天与阶段复盘

新增网站标签页：

```text
每日记录
最近7天
30天与阶段
数据管理
```

30天页面展示：

- 记录次数；
- 实际练习次数；
- 主动决定不练次数；
- 总分钟；
- 超审查上限次数；
- 黄色/红色事件；
- 30个自然日分布；
- 四类基础能力“记录支持状态”；
- 阶段方向；
- 30天 AI 复盘材料。

30天分布严格区分：

```text
有实际练习
主动决定不练
无记录
```

其中“无记录”只表示本机没有该日日志，不能推断当天一定没有练习。

### 3. 四类基础能力

只观察：

1. 身体适应；
2. 呼吸自然度；
3. 注意发现与返回；
4. 日用迁移。

允许的机器状态只有：

```text
记录支持稳定
记录支持部分稳定
尚未稳定
记录不足
因安全原因暂停
```

这些不是修炼认证，也不是医学评估。

### 4. 阶段方向

系统只允许：

- 继续当前基础阶段并观察；
- 继续当前阶段，先补足记录；
- 优先回退、缩短或暂停相关单元；
- 回到已审查负荷后再评估；
- 因安全原因暂停并处理异常；
- 可以讨论下一阶段或分流。

安全优先级固定为：

```text
红色事件
↓
现实功能影响 / 重复黄色 / 多项尚未稳定
↓
记录是否充分
↓
是否反复超出审查负荷
↓
才允许讨论下一阶段或分流
```

即使出现“可以讨论下一阶段或分流”，页面仍明确：

> 这不是自动晋级，也不会自动解锁更高风险内容。

可讨论方向仍为：静修深化 / 导引深化 / 生活养修 / 丹道研究；丹道继续 `research_only`。

### 5. AI 长期复盘

30天 AI 摘要只包含：

- 结构化统计；
- 四类能力状态；
- 阶段方向；
- 安全边界。

默认不包含用户原始长备注。

AI 继续禁止：

- 医疗诊断；
- 气/经络/丹田判断；
- 境界判断；
- 综合修为评分；
- 把不练写成失败；
- 把超时写成进步；
- 把“讨论分流”写成晋级/认证；
- 自动生成 S2—S4 教程。

### 6. schema 迁移保护

当前继续保持：

```text
STORAGE_KEY = wendaozhi.practice.records.v1
SCHEMA_VERSION = 1
```

没有为了版本号而制造无意义的 v2。

新增：

`practice-migrations.mjs`

负责：

- 当前版本原样读取；
- 非法 JSON 明确报错，不伪装成空数据；
- 未来 schema 版本拒绝自动降级；
- 缺少 `vN → vN+1` 迁移函数时停止；
- 迁移必须逐版本执行；
- 迁移后再调用 `normalizeRecord`；
- 迁移失败时不自动覆盖原始 localStorage。

未来真正发布 v2 前，必须先增加显式：

```text
[1, migrateV1ToV2]
```

并通过自动化测试。

### 7. 自动回归

当前 `npm run test:practice` 会运行：

```text
tests/practice-journal.test.mjs
+ tests/practice-migrations.test.mjs
```

共 **26 个自动实践测试**：

- 20 个记录/统计/安全/AI/30天阶段业务场景；
- 6 个 schema 迁移场景。

典型覆盖：

- 正常记录；
- 主动决定不练；
- 超时仍如实保存；
- 红色事件最高优先；
- 重复呼吸控制；
- 30天三态分布；
- 记录不足禁止自动分流；
- 稳定记录只允许“讨论分流”；
- 多次超时回到审查负荷；
- AI 阶段摘要不带私人长备注；
- 非法 JSON；
- 未来 schema 拒绝降级；
- 缺失迁移链；
- 显式迁移保持纯函数边界。

### 8. 独立质量门

`npm run practice:check` 现在检查：

- 八张 S1 实践卡；
- 实践关系与 V3 知识端点；
- 六个规则模块；
- 30天阶段复盘规范；
- schema 迁移规范；
- 本地读取是否走迁移器；
- JSON 导入是否走迁移器；
- 未来版本保护；
- 迁移测试是否存在；
- `npm test` 是否覆盖全部实践测试；
- 客户端无自动网络上传；
- PracticeJournal 真实挂载。

---

## 当前已经机器验证

最新完整 GitHub Actions 已通过：

- V3 图谱一致性；
- 实践体系一致性；
- 旧网站发布回归；
- 26 个实践与迁移测试；
- VitePress 构建；
- 8 张实践详情生成；
- 实践详情知识回链产物检查；
- 30天与阶段入口构建检查；
- 关键旧知识页与图谱页产物检查；
- 网站与图谱构建产物上传。

线上冒烟仍因未配置 `WDZ_SMOKE_BASE_URL` 按现有规则跳过。

---

## 当前仍需真实浏览器试跑

机器测试不能替代：

1. 页面刷新后的 localStorage 保留；
2. 正常保存与“今天不练”实际点击；
3. 黄色/红色事件填写体验；
4. 30天标签页在真实数据下的可读性；
5. JSON 文件真实下载/上传；
6. 剪贴板权限拒绝；
7. 删除与清空确认；
8. 手机窄屏；
9. 深色/浅色主题；
10. Chrome / Edge / Safari 差异；
11. 连续真实使用7天、30天后的信息密度和理解成本。

因此八张基础实践卡继续：

```text
status: review
```

---

## 下一步顺序

### P0：真实浏览器试跑

下一阶段最优先，不再继续堆规则模块。目标是把真实交互问题记录成产品审查项并修正。

### P1：实践工作台体验优化

根据试跑结果考虑：

- 记录表单减负；
- “今日修持”入口；
- 更清楚的当前阶段展示；
- 30天分布可读性；
- 安全提醒信息层级；
- 记录与实践卡之间的快捷跳转。

### P2：深化路线研究

- 静修深化；
- 导引深化；
- 生活养修；
- 丹道研究。

深化路线在研究与安全门完成前，不自动开放为升级教程。

---

## 暂不启动

- 云端个人记录；
- 自动账号体系；
- 内置远程 AI 自动读取日志；
- 自动医学分诊；
- 自动判定黄色/红色严重程度；
- 自动晋级或境界判断；
- 无实际字段需求的 schema v2；
- S2—S4 练习教程。
