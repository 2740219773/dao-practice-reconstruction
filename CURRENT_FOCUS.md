# 当前聚焦工作包

当前工作包：PRACTICE-007「7天首轮真实试运行与产品减法」  
当前状态：V2.7 第三轮“异常与安全渐进展开”完成，进入真实 7 天使用观察  
当前基线：V3.8 知识图谱稳定；8 张 S1 基础实践卡；7天/30天复盘；schema v1；38 个实践/试运行测试；4 条 Chromium CI 入口；Cloudflare Pages 当前提交验证  
当前分支：main  
线上站点：`https://wendaozhi.pages.dev/`

## 一、当前阶段判断

项目不再以“继续补功能”为主。

当前实践闭环已经成立：

```text
今日修持
↓
实践卡
↓
本地修持记录
↓
最近7天复盘
↓
30天记录支持判断
↓
继续 / 回退 / 暂停 / 讨论分流
```

V2.7 的工作方式固定为：

> 真实使用 → 找摩擦 → 找误导 → 找低价值字段 → 做减法。

不再以功能数量、字段数量、实践卡数量作为进展指标。

---

## 二、知识与实践稳定基线

### 知识层

V3.8 继续稳定维护：

- 38 个正式节点；
- 79 条规范关系；
- 历史方法、概念、经典与现代实践继续分层；
- `method.*` 不自动转成现代练习教程；
- `practice.*` 继续强制 `historical_equivalence: false`。

### 实践层

仍只开放 8 张 S1 低风险基础卡：

1. 准备与安全检查；
2. 调身与舒适姿势；
3. 身体接触觉察；
4. 自然察息；
5. 注意返回与收心基础；
6. 短时基础安坐；
7. 轻柔动静转换；
8. 日用觉察。

第一批卡继续保持 `status: review`，CI 通过不等于内容自动转为正式发布。

---

## 三、V2.7 已完成的三轮减法

### 第一轮：产品观察退出主流程

`7天产品观察` 默认折叠为：

> 可选 · 约20秒 · 只记录系统是否好用

如果当天没有使用工作台，只保留日期、被催升级/打卡感和一句产品备注；不再要求填写不存在的耗时、入口和字段摩擦。

产品观察继续独立存储：

```text
wendaozhi.practice.trial.v1
```

不进入修持记录、7天/30天判断或 AI 修持摘要。

### 第二轮：今日首屏退出低价值和进度语义

已经完成：

- “阶段方向”退出今日卡，只在 30 天复盘页出现；
- “情绪状态”退出日常必填，新记录默认 `not_observed`；
- “下次决定”退出日常必填，新记录默认 `not_decided`；
- 保留 schema v1，不为 UI 减法制造 schema v2。

原则：

```text
不知道 → 未记录
没有决定 → 未决定
```

而不是由系统替用户写成“稳定”或“继续”。

### 第三轮：异常与安全改为渐进展开

以前每日记录会直接铺开 11 项异常和安全分流，即使一次完全正常的基础练习也要面对整块异常表。

现在正常状态只显示一行：

> 异常与安全（没有则跳过）

用户可以主动展开完整异常与安全区；系统不会因为折叠而删除任何安全字段。

出现以下情况之一时，异常区自动展开：

- 已勾选本次问题；
- 已选择黄色或红色安全分流；
- 草稿安全规则已经产生提醒；
- 明显主动控制呼吸；
- 练后现实功能受影响；
- 因观察项出现需要重点核对的停止状态。

自动展开后的提示为：

> 需要安全核对 · 已展开

**自动展开不等于自动判定。** 渐进展开层不得替用户选择黄色或红色，也不做医学诊断。

---

## 四、“今天决定不练”的数据边界

“今天决定不练”是一条有效记录，但不是一次已经发生的练习。

因此保存前会强制清理：

```text
durationMinutes → 0
postureState → not_observed
breathState → not_observed
attentionState → not_practiced
emotionState → not_observed
afterState → normal
issues → []
severity → none
nextStep → not_decided
```

这样可以避免：先填写/勾选了一次练习异常，后来改成“今天不练”，旧异常仍残留在记录中。

如果用户是因为当天身体或生活状态不适合而决定不练，可以写在“事实记录”里；系统不会把它伪装成一次已经发生的练习异常。

---

## 五、当前日常入口应只回答什么

第一次或当天打开 `/practice/`，优先只回答：

```text
今天是否适合练？
↓
今天从哪张卡开始？
↓
实际练了多久？
↓
这张卡真正需要观察什么？
↓
有异常吗？
  ├─ 没有 → 跳过异常区
  └─ 有 / 规则提醒 → 展开安全核对
↓
保存事实
```

日常入口不主动制造：

```text
境界
修为分
连续打卡
自动升级
默认“继续”
默认“情绪稳定”
自动黄色 / 红色判定
```

---

## 六、自动化基线

### 38 个实践 / schema / 试运行测试

当前仍为 38 个自动场景，没有为了本轮 UI 变化虚增测试数量。

本轮扩展了既有“主动不练”测试，额外验证：

- 练习观察被清理；
- `issues` 被清空；
- `severity` 回到 `none`；
- `nextStep` 回到 `not_decided`。

### 4 条 Chromium CI 入口

CI 顶层仍为 4 个浏览器步骤：

1. `test:e2e:practice`
   - 通用工作台、JSON、剪贴板、确认框、安全、移动端。
2. `test:e2e:daily`
   - 今日入口与双向实践导航；
   - 第二轮首屏减法；
   - 第三轮异常安全渐进展开。
3. `test:e2e:trial`
   - 7天产品观察隔离与减负。
4. `test:e2e:theme`
   - 日读/夜读、持久化、ARIA、390px 移动端。

其中 `test:e2e:daily` 当前实际串行运行：

```text
e2e-practice-daily.mjs
↓
e2e-practice-reduction.mjs
↓
e2e-practice-safety-reduction.mjs
```

第三段真实 Chromium 验证：

```text
正常记录 → 异常区折叠
↓
手动展开 → 11项异常与安全分流可见
↓
自然察息设为明显主动控制呼吸
↓
草稿安全提醒出现
↓
异常区自动展开
↓
不自动选择黄色/红色
↓
改成“今天决定不练”
↓
异常区退出
↓
保存后 issues=[] / severity=none / nextStep=not_decided
```

### 质量门

`npm run practice:check` 继续由两层组成：

```text
检查实践体系.mjs
+
检查实践减负.mjs
```

减负质量门现在同时要求：

- 不虚构情绪和下次决定；
- 正常状态必须折叠异常列表与安全分流；
- 需要安全核对时必须可以自动展开；
- 渐进展开层不得自动设置 yellow / red；
- 同步逻辑必须幂等，避免 MutationObserver 自触发循环；
- 不练记录必须清理本次练习异常；
- 真实 Chromium 必须覆盖上述行为。

### Cloudflare Pages

所有主分支改动继续等待：

`https://wendaozhi.pages.dev/build-info.json`

追上当前 GitHub SHA 后才执行线上冒烟。

第三轮核心提交 `c723957f` 的 GitHub Actions 已完整通过：图谱、实践质量门、38 个自动场景、网站构建、全部 Chromium、构建产物、Cloudflare 当前提交与线上验证均成功。

---

## 七、现在真正要做的事

### P0：真实 7 天使用

按 `33-实践体系/7天首轮试运行方案-V1.0.md` 正常使用。

重点观察：

- 正常记录是否已经足够短；
- “异常与安全（没有则跳过）”是否比直接铺开更自然；
- 真出现异常时，展开是否足够醒目且不妨碍事实记录；
- 哪些字段仍然长期没有信息价值；
- 7天/30天复盘哪些内容实际上会看；
- 哪些文案仍然像等级或考核；
- 产品观察本身是否值得继续保留。

### P1：真实设备差异

仍待：

- Edge；
- Safari；
- iOS Safari；
- Android Chrome；
- 真机 JSON 下载 / 导入；
- 夜读真实视觉；
- 屏幕阅读器与键盘焦点。

---

## 八、继续冻结

真实试运行数据回来之前继续冻结：

- 新增基础实践卡；
- S2—S4 自学教程；
- 云端个人记录；
- 自动账号体系；
- 远程 AI 自动读取日志；
- 自动医学分诊；
- 自动严重程度判定；
- 自动晋级或境界判断；
- 无真实字段需求的 schema v2；
- 静修深化 / 导引深化 / 丹道实修自动开放。
