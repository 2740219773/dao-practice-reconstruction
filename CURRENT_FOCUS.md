# 当前聚焦工作包

当前工作包：PRACTICE-003「30天长期复盘、阶段判断、浏览器验证与记录迁移保护」
当前状态：V2.3 自动化与线上部署验证基线完成，进入真实长期使用与多浏览器体验阶段
当前基线：V3.8 知识图谱稳定；V2.0 基础实践 8 张；V2.3 已具备7天复盘、30天阶段复盘、知识回链、schema迁移保护、Chromium端到端和Cloudflare Pages线上校验
当前分支：main
线上站点：`https://wendaozhi.pages.dev/`

## 当前状态四句话

- **知识层保持稳定：** V3.8 继续维持 38 个正式节点、79 条规范关系，现代实践顺序仍不进入历史图谱。
- **长期复盘已落地：** 实践工具提供每日记录、最近7天复盘、30天分布、四类基础能力状态与阶段方向。
- **阶段判断不等于晋级：** 系统只输出“记录支持稳定 / 部分稳定 / 尚未稳定 / 记录不足 / 因安全原因暂停”，即使数据充分也只允许“讨论分流”。
- **工程验证已形成闭环：** 26个实践测试 + 真实Chromium端到端 + Cloudflare Pages当前提交线上冒烟，已经纳入主分支CI。

---

## V2.3 已完成

### 1. 六个独立规则模块

```text
practice-model.mjs       数据模型 / 草稿校验 / 导入导出
practice-migrations.mjs  schema 版本识别 / 显式迁移 / 未来版本保护
practice-stats.mjs       7天/30天窗口与聚合统计
practice-safety.mjs      安全规则与填写期提醒
practice-ai.mjs          7天/30天 AI 摘要边界
practice-stage.mjs       30天分布 / 四类能力 / 阶段方向
```

`PracticeJournal.vue` 主要负责界面、本地存储调用和用户交互，不再独自持有全部业务规则。

### 2. 30天与阶段复盘

网站实践工作台现在包含：

```text
每日记录
最近7天
30天与阶段
数据管理
```

30天页面展示：

- 记录次数；
- 实际练习次数；
- 主动决定不练次数；
- 总分钟；
- 超审查上限次数；
- 黄色/红色事件；
- 30个自然日分布；
- 四类基础能力“记录支持状态”；
- 阶段方向；
- 30天 AI 复盘材料。

30天分布严格区分：

```text
有实际练习
主动决定不练
无记录
```

其中“无记录”只表示本机没有该日日志，不能推断当天一定没有练习。

### 3. 四类基础能力

只观察：

1. 身体适应；
2. 呼吸自然度；
3. 注意发现与返回；
4. 日用迁移。

机器状态只有：

```text
记录支持稳定
记录支持部分稳定
尚未稳定
记录不足
因安全原因暂停
```

这些不是修炼认证，也不是医学评估。

### 4. 阶段方向

系统只允许：

- 继续当前基础阶段并观察；
- 继续当前阶段，先补足记录；
- 优先回退、缩短或暂停相关单元；
- 回到已审查负荷后再评估；
- 因安全原因暂停并处理异常；
- 可以讨论下一阶段或分流。

安全优先级固定：

```text
红色事件
↓
现实功能影响 / 重复黄色 / 多项尚未稳定
↓
记录是否充分
↓
是否反复超出审查负荷
↓
才允许讨论下一阶段或分流
```

即使出现“可以讨论下一阶段或分流”，页面仍明确：

> 这不是自动晋级，也不会自动解锁更高风险内容。

可讨论方向仍为：静修深化 / 导引深化 / 生活养修 / 丹道研究；丹道继续 `research_only`。

### 5. AI 长期复盘

30天 AI 摘要只包含：

- 结构化统计；
- 四类能力状态；
- 阶段方向；
- 安全边界。

默认不包含用户原始长备注。

AI 继续禁止：

- 医疗诊断；
- 气/经络/丹田判断；
- 境界判断；
- 综合修为评分；
- 把不练写成失败；
- 把超时写成进步；
- 把“讨论分流”写成晋级/认证；
- 自动生成 S2—S4 教程。

### 6. schema 迁移保护

当前仍保持：

```text
STORAGE_KEY = wendaozhi.practice.records.v1
SCHEMA_VERSION = 1
```

没有为了版本号而制造无意义的 v2。

`practice-migrations.mjs` 负责：

- 当前版本原样读取；
- 非法 JSON 明确报错，不伪装成空数据；
- 未来 schema 版本拒绝自动降级；
- 缺少 `vN → vN+1` 迁移函数时停止；
- 迁移必须逐版本执行；
- 迁移后再调用 `normalizeRecord`；
- 迁移失败时不自动覆盖原始 localStorage。

未来真正发布 v2 前，必须先增加显式：

```text
[1, migrateV1ToV2]
```

并通过自动化测试。

### 7. 26个规则与业务自动测试

`npm run test:practice` 当前运行：

```text
tests/practice-journal.test.mjs
tests/practice-migrations.test.mjs
```

共 **26 个自动实践测试**：

- 20 个记录/统计/安全/AI/30天阶段业务场景；
- 6 个 schema 迁移场景。

覆盖包括：正常记录、主动不练、真实超时、红色优先、重复呼吸控制、30天三态分布、记录不足禁止自动分流、稳定记录只允许讨论分流、多次超时回退、AI隐私边界、未来 schema 拒绝降级、迁移链缺失等。

### 8. Chromium真实端到端

新增：

```text
npm run test:e2e:practice
```

GitHub Actions 使用真实无头 Chromium 验证：

- 实践页实际加载；
- 四个标签页存在；
- 真实表单保存；
- `localStorage` 写入；
- 页面刷新后记录保留；
- 数据管理页显示保留记录；
- 记录不足时30天页不自动升级；
- 红色事件真实保存；
- 红色事件后阶段判断优先暂停；
- 390px 窄屏无明显页面横向溢出。

首版端到端脚本曾出现进程没有正常结束，已经补硬超时、CDP等待超时和显式进程收尾，不通过删除测试来解决。

### 9. Cloudflare Pages线上部署验证

主分支 GitHub 更新后自动部署到：

```text
https://wendaozhi.pages.dev/
```

CI 现在不再依赖手工配置 `WDZ_SMOKE_BASE_URL`，而是默认验证该站点。

流程：

```text
GitHub 当前提交
↓
轮询线上 /build-info.json
↓
等待 Cloudflare Pages commit 追上 github.sha
↓
验证线上首页 / 知识 / 专题 / 安全 / 实践工作台 / 实践卡
↓
验证旧路径与规范URL重定向
```

第一次生产验证实际发现 `.html` 实践卡 URL 会由 Cloudflare 返回 308 并规范化到无扩展名路径；测试已改成验证用户实际使用的规范 URL，同时保留 `.html` 重定向断言。

详细见：

`33-实践体系/浏览器与线上部署验证-V1.0.md`

### 10. CI并发保护

连续提交时，旧工作流不再继续等待已经过期的部署：

```text
concurrency:
  group: website-${{ github.ref }}
  cancel-in-progress: true
```

保证只有当前最新提交需要完成整条部署验证链。

---

## 当前已经机器验证

最新完整 GitHub Actions 已通过：

- V3 图谱一致性；
- 实践体系一致性；
- 旧网站发布回归；
- 26 个实践与迁移测试；
- VitePress 构建；
- Chromium 实践工作台端到端；
- 8 张实践详情生成；
- 实践详情知识回链产物检查；
- 30天与阶段入口构建检查；
- Cloudflare Pages 等待当前提交部署；
- 线上首页、专题、知识、问题地图、安全边界；
- 线上实践工作台与自然察息实践卡；
- 旧 `/library/` 重定向；
- `.html` 实践卡规范化重定向；
- 网站与图谱构建产物上传。

---

## 当前仍需真实长期与多浏览器验证

自动化已经覆盖一部分“真实浏览器试跑”，但仍不能代替真实长期使用。

剩余重点：

1. “今天决定不练”的完整真实交互体验；
2. 黄色事件填写是否容易理解；
3. JSON 文件真实下载、文件选择和重新导入；
4. 剪贴板权限拒绝；
5. 删除与清空确认框；
6. 深色/浅色主题视觉检查；
7. Edge；
8. Safari；
9. iOS Safari / Android Chrome；
10. 键盘导航与无障碍；
11. 连续真实使用7天、30天后的信息密度和理解成本；
12. 用户是否会误解“记录支持稳定”为修炼认证。

因此八张基础实践卡继续：

```text
status: review
```

---

## 下一步顺序

### P0：真实长期使用与多浏览器体验

不再从“页面能不能打开”开始，而是检查浏览器权限操作、不同设备、不同主题、长期数据量和真实使用理解成本。

### P1：实践工作台体验优化

根据真实使用结果考虑：

- 记录表单减负；
- “今日修持”入口；
- 更清楚的当前阶段展示；
- 30天分布可读性；
- 安全提醒信息层级；
- 记录与实践卡之间的快捷跳转。

### P2：深化路线研究

- 静修深化；
- 导引深化；
- 生活养修；
- 丹道研究。

深化路线在研究与安全门完成前，不自动开放为升级教程。

---

## 暂不启动

- 云端个人记录；
- 自动账号体系；
- 内置远程 AI 自动读取日志；
- 自动医学分诊；
- 自动判定黄色/红色严重程度；
- 自动晋级或境界判断；
- 无实际字段需求的 schema v2；
- S2—S4 练习教程。
