# 当前聚焦工作包

当前工作包：PRACTICE-007「7天首轮真实试运行与产品减法」  
当前状态：V2.7 第二轮首屏减法完成，进入真实 7 天使用观察  
当前基线：V3.8 知识图谱稳定；8 张 S1 基础实践卡；7天/30天复盘；schema v1；38 个实践/试运行测试；4 条 Chromium CI 入口；Cloudflare Pages 当前提交验证  
当前分支：main  
线上站点：`https://wendaozhi.pages.dev/`

## 一、当前阶段判断

项目已经不再处于“继续补功能”的阶段。

当前实践闭环已经成立：

```text
今日修持
↓
实践卡
↓
本地修持记录
↓
最近7天复盘
↓
30天记录支持判断
↓
继续 / 回退 / 暂停 / 讨论分流
```

V2.7 的核心任务改为：

> 真实使用 → 找摩擦 → 找误导 → 找低价值字段 → 做减法。

不再以“功能数量、字段数量、实践卡数量”作为进展指标。

---

## 二、知识与实践基线

### 知识层

继续保持 V3.8：

- 38 个正式节点；
- 79 条规范关系；
- 历史方法、概念、经典与现代实践层继续分离；
- `method.*` 不自动生成现代教程；
- `practice.*` 继续强制 `historical_equivalence: false`。

### 实践层

第一批仍只开放 8 张 S1 基础卡：

1. 准备与安全检查；
2. 调身与舒适姿势；
3. 身体接触觉察；
4. 自然察息；
5. 注意返回与收心基础；
6. 短时基础安坐；
7. 轻柔动静转换；
8. 日用觉察。

不新增 S2—S4 自学教程。

---

## 三、V2.7 已完成的第一轮减法

### 产品观察退出主流程

`7天产品观察` 现在默认折叠为：

> 可选 · 约20秒 · 只记录系统是否好用

如果当天没有使用实践工作台，只保留：

- 日期；
- 是否出现“系统在催我升级/打卡”的感觉；
- 一句话产品备注。

不再要求填写不存在的：

- 操作耗时；
- 主要入口；
- 难选字段；
- 低价值字段。

产品观察继续使用独立存储：

```text
wendaozhi.practice.trial.v1
```

不会进入修持记录、7天/30天判断或 AI 修持摘要。

---

## 四、V2.7 第二轮首屏减法

这轮针对“今日修持”和每日记录继续做减法。

### 1. “阶段方向”退出今日卡

以前今日卡会显示：

```text
阶段方向：继续当前基础阶段并观察
```

虽然技术上不是自动升级，但日常入口里出现“阶段方向”仍容易制造等级/进度感。

现在：

- 今日卡只回答“今天从哪里开始”和“今天有没有记录”；
- 阶段信息只在 `30天与阶段` 页面出现；
- 安全红/黄状态仍优先展示，不受此次减法影响。

### 2. “情绪状态”退出日常必填

当前情绪字段：

- 不参与最近7天安全规则；
- 不参与30天基础能力判断；
- 以前默认值是“稳定”。

这会造成用户没有主动填写时，系统仍保存一条看似真实的“情绪稳定”。

现在新记录默认：

```text
emotionState: not_observed
```

旧 schema 字段保留，历史记录继续兼容，但日常表单不再主动展示该字段。

### 3. “下次决定”退出日常必填

以前新记录默认：

```text
nextStep: continue
```

这相当于用户什么都没选，系统就替用户写下“下次继续”。

现在新记录默认：

```text
nextStep: not_decided
```

该字段继续保留用于兼容旧记录，但不再占据每日记录入口。

### 4. 不升级 schema

本轮没有创建 schema v2。

原因：

- 旧字段仍可兼容；
- 新值只是更诚实的“未观察 / 未决定”；
- 没有真实迁移需求；
- 不为了版本号制造数据升级成本。

---

## 五、当前日常使用原则

第一次或当天打开 `/practice/` 时，系统只应该优先回答：

```text
今天是否适合练？
↓
今天从哪张卡开始？
↓
实际练了多久？
↓
这张卡真正需要观察什么？
↓
有没有不适 / 异常？
↓
保存事实
```

不应该在日常入口主动制造：

```text
境界
阶段排名
修为分
自动升级
默认“继续”
默认“情绪稳定”
必须连续打卡
```

---

## 六、自动化基线

### 38 个实践 / schema / 试运行测试

当前包括：

- 原有修持记录、统计、安全、30天阶段与 schema 场景；
- 产品观察隔离与减负场景；
- 新增“新记录默认不虚构情绪状态或下次决定”场景；
- 主动不练会清理无关练习观察和默认情绪值。

### Chromium E2E

CI 仍保留 4 个浏览器步骤：

1. `test:e2e:practice`
   - 通用工作台、JSON、剪贴板、确认框、安全、移动端。
2. `test:e2e:daily`
   - 今日入口、继续上次、不练、双向实践卡、安全覆盖、按卡最小字段；
   - 同一步骤追加首屏减法 Chromium：阶段方向隐藏、情绪/下次决定退出日常表单、保存值保持 `not_observed / not_decided`。
3. `test:e2e:trial`
   - 产品观察默认折叠、未使用时减字段、独立保存和清空隔离。
4. `test:e2e:theme`
   - 日读/夜读、持久化、ARIA、390px 移动端。

### 质量门

`npm run practice:check` 现在由两层组成：

```text
检查实践体系.mjs
+
检查实践减负.mjs
```

第二层硬检查：

- `emotionState` 默认必须是 `not_observed`；
- `nextStep` 默认必须是 `not_decided`；
- 今日卡不得主动显示阶段方向；
- 日常表单不得主动显示情绪/下次决定；
- 真实 Chromium 必须验证最终本地 JSON 没有虚构默认结论。

### Cloudflare Pages

所有改动继续等待：

`https://wendaozhi.pages.dev/build-info.json`

追上当前 GitHub SHA 后才执行线上验证。

第二轮减法代码提交 `f046274f` 已通过：

- V3 图谱检查；
- 实践体系 + 实践减负质量门；
- 38 个自动场景；
- 网站构建；
- 全部 Chromium E2E；
- 构建产物检查；
- Cloudflare 当前提交与线上冒烟验证。

---

## 七、现在真正要做的事

### P0：真实7天使用

按 `33-实践体系/7天首轮试运行方案-V1.0.md` 正常使用。

真正收集：

- 打开后是否立即知道该做什么；
- 保存一次记录需要多少额外操作；
- 哪些字段仍然长期没有信息价值；
- 哪些复盘信息实际不会看；
- 哪些词仍然像等级或考核；
- “今天不练”是否足够自然；
- 产品观察本身是否还有存在价值。

### P0：继续做减法，不做功能扩张

如果真实数据表明：

- 某字段长期没用 → 删除或折叠；
- 某块复盘长期不看 → 后移；
- “阶段”仍像晋级 → 改语义；
- 今日入口仍犹豫 → 减少选择；
- 产品观察本身太烦 → 继续缩减甚至移除。

### P1：真实设备差异

仍待：

- Edge；
- Safari；
- iOS Safari；
- Android Chrome；
- 真机 JSON 下载 / 导入；
- 夜读真实视觉；
- 屏幕阅读器与键盘焦点。

---

## 八、继续冻结

在真实试运行数据回来之前，继续冻结：

- 新增基础实践卡；
- S2—S4 自学教程；
- 云端个人记录；
- 自动账号体系；
- 远程 AI 自动读取日志；
- 自动医学分诊；
- 自动严重程度判定；
- 自动晋级或境界判断；
- 无真实字段需求的 schema v2；
- 静修深化 / 导引深化 / 丹道实修的自动开放。
