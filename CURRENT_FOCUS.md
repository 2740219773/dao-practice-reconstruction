# 当前聚焦工作包

当前工作包：PRACTICE-002「本地记录、复盘与实践关系模型」
当前状态：第一轮 MVP 已完成，进入真实使用试跑
当前基线：V3.8 知识图谱稳定；V2.0 基础实践 8 张；本地记录/7天复盘/AI复盘材料/实践关系质量门已落地
当前分支：main

## 当前状态四句话

- **知识层保持稳定：** V3.8 继续维持 38 个正式节点、79 条规范关系，历史知识关系不混入现代教学顺序。
- **实践记录已可用：** `/practice/` 已提供浏览器本地记录、最近7天复盘、JSON导入导出和数据清空；默认不上传。
- **AI 只做复盘材料：** 页面只生成可复制摘要，不自动发送，不诊断疾病、不判境界、不生成 S2—S4 教程。
- **实践关系单独治理：** `prerequisite_for / contextualized_by / not_equivalent_to / safety_constrained_by` 只在实践层使用，并由独立 CI 质量门校验。

---

## PRACTICE-002 已完成

### 1. 实践记录数据模型 V1.0

版本化存储：

```text
wendaozhi.practice.records.v1
schema_version: 1
```

记录：

- 日期；
- 实践卡；
- 实际时长；
- 开始前状态；
- 姿势；
- 呼吸自然度；
- 注意返回；
- 情绪；
- 练后状态；
- 问题；
- 黄色/红色分流；
- 调整；
- 事实备注；
- 下次决定。

### 2. 本地优先交互

当前网页支持：

- 保存到浏览器 `localStorage`；
- 最近 7 天统计；
- 重复问题识别；
- 黄色/红色事件统计；
- 规则型安全提示；
- JSON 导出；
- JSON 导入；
- 删除单条；
- 清空全部。

默认：

```text
无账号
无云同步
无后台自动上传
```

### 3. AI 实践复盘规范 V1.0

AI 输出约束：

```text
本周事实
→ 重复模式
→ 安全提醒
→ 下周基础建议
→ 不能判断的部分
```

禁止：

- 医疗诊断；
- 气/经络/丹田机制判断；
- 境界判断；
- S2—S4 自动教程；
- 红色信号后继续推荐练法；
- 无异常时自动增加时长。

### 4. 知识—实践关系模型 V1.0

允许：

- `prerequisite_for`：现代教学依赖；
- `contextualized_by`：传统背景关联；
- `not_equivalent_to`：显式不等同；
- `safety_constrained_by`：安全约束。

当前 `实践关系-v1.json` 只属于实践层，不注入 V3 历史图谱。

### 5. 独立质量门

CI 新增：

```text
npm run practice:check
```

检查：

- 8 张卡字段；
- ID/slug/sequence；
- S1/B/basic_low_risk；
- 停止条件与完成标准；
- 实践关系类型；
- V3 知识端点；
- 本地存储版本；
- 是否存在自动网络上传代码；
- 实践页组件挂载。

第一次质量门暴露“graph:check 不写 data/”的依赖问题，已修为直接扫描 V3 源 Markdown；修正后 CI 全绿。

---

## 第一轮产品审查发现并已修

1. **超出建议时长仍应允许如实记录**，不能为了合规让真实数据消失；
2. 数据模型已有 `emotionState`，UI 必须实际输入，不能默认伪造“稳定”；
3. JSON 导入只接受当前 schema v1 已知实践 ID；
4. 实践质量门必须直接验证源数据，不依赖其他检查的构建副产物。

详细见：

`33-实践体系/基础实践体系第一轮产品审查-2026-08-12.md`

---

## 当前下一步：真实使用试跑

P0 不是继续增加练法，而是用实际浏览器跑完整闭环：

1. 正常保存一次；
2. 记录“今天不练”；
3. 如实保存一次超过卡片审查上限的记录；
4. 连续两次记录主动控制呼吸；
5. 检查 7 天复盘规则；
6. 记录黄色事件；
7. 记录红色事件；
8. 导出再导入；
9. 删除与清空；
10. 移动端与深浅主题检查。

---

## 后续顺序

### P1：拆分记录逻辑

试跑稳定后，把 schema 校验、数据迁移、周统计、安全规则、AI摘要从 Vue 组件拆成独立纯函数模块并做单元测试。

### P1：30天/阶段复盘

增加长期趋势视图，但仍不生成综合分数。

### P2：实践详情的知识回链

实践卡详情页增加：

- 传统背景；
- 明确不等同；
- 安全约束；
- 继续学习。

### P2：深化路线

- 静修深化；
- 导引深化；
- 生活养修；
- 丹道研究。

丹道继续默认 `research_only`。

---

## 暂不启动

- 云端个人记录；
- 自动账号体系；
- 内置远程 AI 自动读取日志；
- 自动医学分诊；
- 自动判定黄色/红色严重程度；
- 实践完成后自动升级境界；
- S2—S4 练习教程。
