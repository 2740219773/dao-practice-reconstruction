# 当前聚焦工作包

当前工作包：PRACTICE-002「本地记录、复盘与实践关系模型」
当前状态：V2.2 自动回归基线完成，进入真实浏览器试跑与长期复盘设计
当前基线：V3.8 知识图谱稳定；V2.0 基础实践 8 张；V2.2 记录规则模块化、14个自动场景、实践详情知识回链已落地
当前分支：main

## 当前状态四句话

- **知识层保持稳定：** V3.8 继续维持 38 个正式节点、79 条规范关系，现代实践顺序仍不进入历史图谱。
- **记录逻辑已模块化：** 数据模型、7天统计、安全规则、AI摘要已从 `PracticeJournal.vue` 拆成独立纯函数模块。
- **自动场景已建立：** `npm test` 现在覆盖 14 个实践记录/导入导出/统计/安全/AI 场景，并继续跑旧网站发布回归。
- **实践详情已能回到知识：** 每张实践详情会从 `实践关系-v1.json` 自动生成前序、下一步、传统背景、明确不等同和安全约束。

---

## PRACTICE-002 已完成

### 1. 实践记录数据模型 V1.0

版本化存储：

```text
wendaozhi.practice.records.v1
schema_version: 1
```

记录字段包括日期、实践卡、实际时长、开始前状态、姿势、呼吸自然度、注意返回、情绪、练后状态、问题、安全分流、调整、事实备注和下次决定。

数据模型现在位于：

`website/docs/.vitepress/theme/practice/practice-model.mjs`

负责：

- 空白记录；
- 草稿校验；
- 实际记录生成；
- 已知实践 ID 校验；
- 问题字段过滤；
- JSON 导出；
- JSON 导入与同 ID 合并。

### 2. 7天统计模块

`practice-stats.mjs` 负责：

- 最近7天本地日期窗口；
- 记录次数；
- 实际练习次数；
- 主动决定不练次数；
- 实际总分钟；
- 黄色/红色事件；
- 重复问题；
- 实践卡使用次数；
- 超过卡片审查上限次数。

“主动决定不练”不计为失败；“实际超时”允许记录，但反复出现会进入安全复盘。

### 3. 安全规则模块

`practice-safety.mjs` 负责：

- 红色事件最高优先级；
- 现实功能影响；
- 重复黄色事件；
- 重复主动控制呼吸；
- 重复姿势问题；
- 多次超过卡片审查时长；
- 填写阶段的安全分流核对。

系统可以提示“请重新核对黄色/红色分流”，但不自动替用户做医学严重程度判断。

### 4. AI 摘要模块

`practice-ai.mjs` 负责：

- 生成7天结构化摘要；
- 生成可复制 AI 复盘提示词；
- 明确“不诊断、不判气/经络/丹田/境界、不生成 S2—S4 教程”；
- 不把个人长备注自动塞入 AI 摘要；
- 不把“今天不练”解释为意志问题；
- 不把超时解释为进步。

### 5. 自动场景回归

测试文件：

`website/tests/practice-journal.test.mjs`

当前 14 个自动场景覆盖：

1. 正常记录；
2. 主动决定不练；
3. 超过卡片审查上限仍如实记录；
4. 超过系统绝对时长边界拒绝；
5. JSON 导入过滤未知数据并合并；
6. JSON 导出只保留规范记录；
7. 最近7天日期边界；
8. 实际练习/不练/超时/黄色事件聚合；
9. 红色事件最高优先级；
10. 重复主动控制呼吸；
11. 重复超时；
12. 高关注异常与“无升级事件”冲突；
13. 自然察息明显主动控制；
14. AI 摘要隐私与边界。

入口：

```text
npm run test:practice
npm test
```

详细：`33-实践体系/实践记录自动场景回归-V1.0.md`。

### 6. 实践关系与详情回链

实践关系继续只使用：

- `prerequisite_for`；
- `contextualized_by`；
- `not_equivalent_to`；
- `safety_constrained_by`。

网站构建器现在从 `实践关系-v1.json` 自动向每张实践详情生成：

```text
前序基础
下一基础
传统与知识背景
明确不等同
安全约束
```

例如“自然察息”会自动链接：

- 息；
- 吐纳（明确不等同）；
- 服气（明确不等同）；
- 安全边界 V0.2。

“轻柔动静转换”会自动链接导引，并明确不等同于古代导引套路复原。

CI 不只检查 JSON，还直接检查最终 HTML 中这些关系是否真实出现。

### 7. 独立质量门

`npm run practice:check` 现在同时检查：

- 8 张实践卡字段和顺序；
- 实践关系与 V3 知识端点；
- 4 个独立规则模块是否存在；
- 版本化本地存储；
- 导入合并规则；
- 统计/安全/AI模块入口；
- `npm test` 是否包含实践场景；
- 客户端是否出现自动网络上传代码；
- PracticeJournal 是否挂载。

本轮重构时质量门曾因旧检查仍假设存储键写在 Vue 组件内而失败；已经修为按模块化架构检查。没有关闭或降低质量门。

---

## 当前已经机器验证

最新完整 CI 已通过：

- V3 图谱一致性；
- 实践体系一致性；
- 旧发布状态回归；
- 14 个实践场景测试；
- VitePress 构建；
- 8 张实践详情生成；
- 实践详情关系回链 HTML 检查；
- 关键旧知识页与图谱页产物检查。

---

## 当前仍需真实浏览器试跑

自动测试不能代替以下实际操作：

1. 页面刷新后本地记录保留；
2. 正常保存与“今天不练”实际点击；
3. 超时记录实际交互；
4. 黄色/红色事件填写体验；
5. JSON 文件真实下载与上传；
6. 剪贴板权限被拒绝；
7. 删除与清空确认；
8. 手机窄屏；
9. 深色/浅色主题；
10. Chrome / Edge / Safari 差异；
11. 连续真实使用7天后的复盘可读性。

因此 8 张实践卡继续保持：

```text
status: review
```

---

## 下一步顺序

### P0：真实浏览器试跑

完成上述实际交互验证，并记录问题；不因为 CI 全绿就直接改为 `published`。

### P1：30天与阶段复盘

在不生成综合分数的前提下增加：

- 30天练习/不练分布；
- 重复异常趋势；
- 哪些基础能力长期稳定/部分稳定/未稳定；
- 阶段决定：继续 / 回退 / 暂停 / 讨论分流。

### P1：schema 迁移机制

在 v1 真正积累用户记录之前，先定义未来 `schemaVersion: 2` 的迁移策略，避免字段升级时丢数据。

### P2：深化路线研究

- 静修深化；
- 导引深化；
- 生活养修；
- 丹道研究。

丹道继续默认 `research_only`。

---

## 暂不启动

- 云端个人记录；
- 自动账号体系；
- 内置远程 AI 自动读取日志；
- 自动医学分诊；
- 自动判定黄色/红色严重程度；
- 实践完成后自动升级境界；
- S2—S4 练习教程。
