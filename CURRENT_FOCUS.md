# 当前聚焦工作包

当前工作包：PRACTICE-004「真实交互、阅读主题与长期使用验证」
当前状态：V2.4 浏览器真实交互基线完成，进入多浏览器与长期真实使用阶段
当前基线：V3.8 知识图谱稳定；8 张 S1 基础实践卡；7天/30天复盘；schema v1 迁移保护；双 Chromium E2E；Cloudflare Pages 当前提交验证
当前分支：main
线上站点：`https://wendaozhi.pages.dev/`

## 当前状态

- **知识层保持稳定：** V3.8 继续维持 38 个正式节点、79 条规范关系，现代实践顺序不进入历史图谱。
- **实践记录已形成长期闭环：** 每日记录 → 7天复盘 → 30天阶段判断 → 回退/暂停/讨论分流。
- **真实浏览器验证继续前移：** 文件导入导出、剪贴板拒绝、删除/清空确认、键盘标签导航、“今天决定不练”、390px 窄屏均已纳入 Chromium。
- **阅读体验新增日读/夜读：** 夜读采用深纸浅墨而非纯黑主题，用户选择保存在本地并经过独立 Chromium 回归。
- **线上部署可追踪：** GitHub main 更新后等待 Cloudflare Pages 的 `build-info.json` 追上当前提交，再验证线上关键页面和路由。

---

## V2.4 本轮完成

### 1. “今天决定不练”减负

选择“今天决定不练”后：

- 时长自动归零；
- 姿势、呼吸、注意、练后状态不再要求填写；
- 仍可记录情绪、安全分流、调整和事实备注；
- 保存后明确提示“不练不会被计为失败，也不会生成补练要求”。

### 2. 本地数据真实操作

真实 Chromium 已验证：

- JSON 导出生成有效 schema v1 文件内容；
- JSON 导入通过真实 `File + DataTransfer + change` 事件进入现有导入链；
- 导出和导入均不绕过现有规范化/迁移规则；
- 单条删除取消确认时不删除；
- 单条删除确认后才删除；
- 清空全部取消确认时不清空；
- 清空确认后才清空；
- 导出成功明确提示只生成本地备份。

### 3. 剪贴板权限降级

真实 Chromium 模拟 `NotAllowedError`：

- 复制失败不抛出未处理异常；
- 不触发网络发送；
- 页面提示用户手动选择文本复制。

### 4. 键盘与基础无障碍

实践工作台标签页现在使用：

```text
role="tablist"
role="tab"
aria-selected
```

并支持：

- ← / → 切换；
- Home 跳到第一项；
- End 跳到最后一项；
- 提示消息使用 `role="status"` + `aria-live="polite"`。

### 5. 日读 / 夜读

顶部导航新增阅读主题按钮：

```text
日读：暖宣纸 + 深墨
夜读：深纸 + 浅墨 + 克制朱砂
```

规则：

- 首次无偏好时参考系统深浅偏好；
- 用户手动切换后写入 `wendaozhi.reading.theme`；
- 页面刷新后恢复选择；
- 移动端仍保留主题按钮；
- `aria-pressed` 和按钮提示随主题同步；
- 不改变实践记录 schema。

独立命令：

```text
npm run test:e2e:theme
```

### 6. 当前 CI 链

```text
图谱一致性
↓
实践体系一致性
↓
26 个实践 / 迁移单元场景
↓
网站构建
↓
Chromium 实践工作台 E2E
↓
Chromium 日读 / 夜读 E2E
↓
静态产物检查
↓
等待 Cloudflare Pages 当前提交
↓
线上冒烟
```

最新完整工作流已全部通过。

---

## 仍保持不变的安全边界

八张基础实践卡继续：

```text
status: review
risk_level: S1
activity_class: B
historical_equivalence: false
```

不因为工程测试全绿而升级为“已验证功法”。

S2—S4 继续不自动开放：主动调息、闭气、胎息、强意守、长时禁动、服气、内丹火候/周天、辟谷、丹药等。

---

## 下一步顺序

### P0：多浏览器与真实设备

重点验证：

- Edge；
- Safari；
- iOS Safari；
- Android Chrome；
- 文件下载/选择在移动端的表现；
- 夜读主题在不同屏幕上的实际视觉效果；
- 键盘焦点可见性与屏幕阅读器基础语义。

### P0：真实 7 天 / 30 天使用

机器数据不能替代长期产品体验。重点观察：

- 每日表单是否仍然太重；
- 黄色/红色分流是否容易理解；
- 30 天信息是否过密；
- 用户是否把“记录支持稳定”误解成境界认证；
- 日用觉察是否比静坐更容易真正坚持；
- 哪些字段长期几乎没人使用，可以删减。

### P1：实践工作台减负

优先考虑：

- “今日修持”快捷入口；
- 根据实践卡只显示相关字段；
- 当前基础阶段更清楚；
- 记录与实践卡快捷跳转；
- 30 天分布更易读；
- 安全提醒层级更清晰。

### P2：深化路线研究

静修深化 / 导引深化 / 生活养修 / 丹道研究继续先研究后开放；丹道默认 `research_only`。

---

## 暂不启动

- 云端个人记录；
- 自动账号体系；
- 内置远程 AI 自动读取日志；
- 自动医学分诊；
- 自动判定黄色/红色严重程度；
- 自动晋级或境界判断；
- 无实际字段需求的 schema v2；
- S2—S4 自学教程。
