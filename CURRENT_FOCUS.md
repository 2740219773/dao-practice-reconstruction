# 当前聚焦工作包

当前工作包：PRACTICE-005「实践工作台减负与长期真实使用」
当前状态：V2.5 今日修持与最小字段基线完成，进入真机、多浏览器与真实7/30天使用阶段
当前基线：V3.8 知识图谱稳定；8 张 S1 基础实践卡；7天/30天复盘；schema v1；31 个实践测试；三条 Chromium E2E；Cloudflare Pages 当前提交验证
当前分支：main
线上站点：`https://wendaozhi.pages.dev/`

## 当前状态

- **知识层保持稳定：** V3.8 继续维持 38 个正式节点、79 条规范关系，现代教学顺序不进入历史图谱。
- **实践层从“能记录”推进到“每天可用”：** `/practice/` 已把“今日修持与记录”前置，完整八张路线退到后方作为查阅材料。
- **不自动升级：** 正常状态只提供“继续上次”，不会根据一次记录自动跳到下一张实践卡；黄色/红色信号优先回到安全检查或安全边界。
- **表单按卡减负：** 每张实践卡在数据模型中声明自己的最小观察字段，隐藏字段在保存前也会被模型清理，避免残留脏数据。
- **工程验证形成三条真实浏览器链：** 通用实践工作台、今日修持减负、日读/夜读分别用真实 Chromium 验证，再等待 Cloudflare Pages 部署当前提交。

---

## V2.5 已完成

### 1. 今日修持入口

实践工作台首部增加“今日修持”：

- 第一次使用从“准备与安全检查”开始；
- 已有正常记录时显示“继续上次”；
- “继续上次”只降低选择成本，不代表晋级；
- 最近7天出现黄色信号时回到准备与安全检查；
- 出现红色信号时不再提供新的练习入口，只保留安全边界与“不练”记录；
- 显示当前30天阶段方向，但明确它只是记录支持判断；
- 显示今天已有多少条记录。

### 2. 工作台前置

`/practice/` 页面顺序改为：

```text
基础边界说明
↓
今日修持与记录工作台
↓
完整八张基础路线
↓
阶段与分流说明
```

日常用户不必每次先滚过八张实践说明。

### 3. 每张实践卡的最小记录字段

统一数据模型现在定义：

| 实践卡 | 额外观察字段 |
| --- | --- |
| 准备与安全检查 | 无 |
| 调身与舒适姿势 | 姿势、练后状态 |
| 身体接触觉察 | 姿势、练后状态 |
| 自然察息 | 呼吸、练后状态 |
| 注意返回与收心基础 | 注意返回、练后状态 |
| 短时基础安坐 | 姿势、呼吸、注意、练后状态 |
| 轻柔动静转换 | 姿势、练后状态 |
| 日用觉察 | 注意返回、练后状态 |

情绪、安全分流、问题项仍属于通用记录。

关键规则：

> 界面隐藏不是数据规则。`createStoredRecord` 保存前会再次清理不属于当前卡的字段。

因此用户先填写短时安坐、再切换到自然察息时，旧姿势/注意值不会悄悄进入新记录。

### 4. “今天决定不练”继续减负

选择“不练”后：

- 时长归零；
- 姿势、呼吸、注意、练后状态全部清理；
- 页面使用最简表单；
- 不计失败；
- 不要求补练；
- 仍可以记录真正需要的安全事件与事实。

### 5. 实践卡快捷回看

今日入口和当前记录头部都提供实践卡直达链接：

```text
practice.basic.natural_breath
→ /practice/card/natural-breath
```

用户可以先查看步骤、停止条件和“传统背景但不等同”的关系，再回来记录。

### 6. 补充备注折叠

“做了什么调整 / 事实记录”移动到“补充记录（可选）”折叠区。

日常记录默认只显示真正需要作出选择的结构化字段，减少每次打开页面的视觉负担。

---

## 自动化基线

### 31 个实践 / schema 测试

`npm run test:practice` 当前共 **31 个**场景：

- 原有 26 个记录、统计、安全、阶段、AI、schema 迁移场景；
- 新增 5 个最小字段与实践卡 URL 场景。

新增断言包括：

- 每张卡声明自己的最小字段；
- 自然察息不会保存姿势/注意残留；
- 短时基础安坐保留完整四类观察；
- “今天不练”清理全部练习观察；
- 实践卡 URL 统一由模型生成。

### 三条 Chromium E2E

1. `npm run test:e2e:practice`
   - 通用工作台、JSON 导入导出、剪贴板拒绝、删除/清空确认、键盘、红色安全暂停、390px 窄屏等。
2. `npm run test:e2e:daily`
   - 今日入口、第一次安全检查、自然察息/短时安坐最小字段、继续上次但不自动前进、不练最简表单、实践卡链接。
3. `npm run test:e2e:theme`
   - 日读/夜读切换、实际纸墨色变化、刷新持久化、ARIA 与移动端入口。

### Cloudflare Pages

CI 最后等待：

`https://wendaozhi.pages.dev/build-info.json`

追上当前 `github.sha`，再检查线上：

- `PRACTICE-004`；
- “今日修持”；
- “30天与阶段”；
- “今天决定不练”；
- 关键知识/专题/安全页面；
- 实践卡规范 URL；
- 旧路径重定向。

最新完整提交 `1b271c2b` 已通过整条链。

---

## 仍保持的安全边界

八张基础实践卡继续：

```text
status: review
risk_level: S1
activity_class: B
historical_equivalence: false
```

工程稳定不等于修炼效果验证。

S2—S4 内容继续不自动开放：主动调息、闭气、胎息、强意守、长时禁动、服气、内丹火候/周天、辟谷、丹药等。

---

## 下一步顺序

### P0：真实7天 / 30天使用

现在最重要的数据已经不是“还能不能编译”，而是：

- 每日最小字段是否仍然嫌多；
- 用户会不会频繁跳过“问题项/安全分流”；
- 今日入口是否真的比原先路线列表更容易使用；
- 7天和30天复盘多久看一次才自然；
- 哪些字段长期几乎没有信息价值；
- “阶段方向”是否仍可能被误读为修炼等级。

### P0：真机 / 多浏览器

继续验证：Edge、Safari、iOS Safari、Android Chrome、真实手机文件导入导出、夜读视觉和屏幕阅读器。

### P1：实践卡 → 工作台反向快捷入口

当前已经实现“工作台 → 实践卡”。下一步可以让每张实践详情带上“记录本次实践”，回到工作台时只**预选**该卡，不自动开始、不自动升级。

### P1：30天信息减负

根据真实使用再决定：

- 是否默认折叠 AI 复盘材料；
- 是否把30天能力卡改成更紧凑布局；
- 是否把无记录日视觉进一步弱化；
- 是否增加“只看异常变化”的视图。

### P2：深化路线研究

静修深化 / 导引深化 / 生活养修 / 丹道研究继续先研究后开放；丹道保持 `research_only`。

---

## 暂不启动

- 云端个人记录；
- 自动账号体系；
- 远程 AI 自动读取日志；
- 自动医学分诊；
- 自动严重程度判定；
- 自动晋级或境界判断；
- 无实际字段需求的 schema v2；
- S2—S4 自学教程。
