# 当前聚焦工作包

当前工作包：PRACTICE-006「双向实践导航与长期真实使用」
当前状态：V2.6 实践卡 ↔ 工作台双向导航基线完成，进入真机、多浏览器与真实7/30天使用阶段
当前基线：V3.8 知识图谱稳定；8 张 S1 基础实践卡；7天/30天复盘；schema v1；31 个实践测试；三条 Chromium E2E；Cloudflare Pages 当前提交验证
当前分支：main
线上站点：`https://wendaozhi.pages.dev/`

## 当前状态

- **知识层保持稳定：** V3.8 维持 38 个正式节点、79 条规范关系，现代教学顺序不进入历史图谱。
- **实践层已经形成日常闭环：** 今日修持 → 实践卡 → 记录 → 7天复盘 → 30天阶段判断 → 回退/暂停/讨论分流。
- **V2.6 补齐双向导航：** 工作台可以查看当前实践卡；每张实践卡也会自动生成“记录本次实践”，回到工作台并预选该卡。
- **预选不是自动执行：** 反向入口不会自动开始、自动保存、增加记录或改变阶段。
- **安全优先于 URL：** 黄色/红色安全状态可以覆盖 `?practice=<slug>`；红色状态下即使从“自然察息”实践卡返回，也强制回到“准备与安全检查”。
- **非法入口不信任：** 未登记 slug 不会创建记录；本地 schema 无法安全读取时也不会继续套用 URL 参数。
- **表单继续按卡减负：** 每张实践卡只显示并保存真正相关的观察字段。

---

## V2.6 本轮完成

### 1. 实践卡 → 工作台

8 张自动生成的实践详情页统一增加：

```text
记录本次实践 →
/practice/?practice=<slug>#practice-journal
```

例如：

```text
自然察息
↓
/practice/?practice=natural-breath#practice-journal
```

进入后只做三件事：

1. 定位到实践工作台；
2. 预选“自然察息”；
3. 按该卡最小字段显示空记录表单。

不会做：

```text
自动开始
自动计时
自动保存
自动晋级
自动增加训练负荷
```

### 2. 工作台 → 实践卡

V2.5 已有的正向链接继续保留：

```text
今日修持 / 当前记录
↓
查看当前实践卡
↓
步骤、停止条件、知识背景与“不等同”边界
```

因此现在形成：

```text
实践卡
  ↕
工作台
```

而不是两个孤立页面。

### 3. 安全覆盖 URL 参数

URL 参数只是界面预选信息，不具备安全决策权。

处理顺序固定为：

```text
读取并规范化本地记录
↓
计算最近7天安全状态
↓
红色 / 黄色优先处理
↓
只有无安全阻断时才接受 practice 参数
```

红色事件存在时：

```text
/practice/?practice=natural-breath
```

不会打开自然察息，而是回到：

```text
practice.basic.precheck
```

并明确提示“实践卡链接不能绕过安全状态”。

### 4. URL 输入边界

- 只接受 `practice-model.mjs` 中登记的 8 个 slug；
- 未知 slug 被忽略；
- 未知 slug 不生成任何本地记录；
- 本地存储损坏、未来 schema 或无法迁移时，优先保护原数据，不执行反向预选；
- URL 参数不会写入 schema，也不会改变现有记录。

### 5. 质量门

`npm run practice:check` 现在额外检查：

- `PracticeJournal` 必须存在 `applyPracticeEntryFromUrl`；
- 必须解析 `URLSearchParams`；
- 必须保留 `practice-journal` 锚点；
- URL 入口必须基于已载入记录独立计算安全状态；
- 实践详情生成器必须生成“记录本次实践”；
- 必须包含 `?practice=` 与 `#practice-journal`；
- 今日修持 Chromium 必须覆盖双向导航；
- Chromium 必须覆盖“实践卡链接不能绕过安全状态”。

---

## 自动化基线

### 31 个实践 / schema 测试

继续覆盖：

- 记录规范化；
- 每张卡最小字段；
- 不练记录；
- 7天统计；
- 30天阶段判断；
- 黄色/红色规则；
- AI 摘要边界；
- schema v1 迁移保护。

### 三条 Chromium E2E

1. `npm run test:e2e:practice`
   - 通用工作台、JSON、剪贴板、删除/清空、键盘、红色暂停、390px 窄屏。
2. `npm run test:e2e:daily`
   - 今日入口、继续上次、不练、按卡最小字段；
   - 实践卡 → 工作台真实点击；
   - 反向预选不增加记录；
   - 带 `practice=` 参数页面刷新后，红色安全状态覆盖参数。
3. `npm run test:e2e:theme`
   - 日读/夜读切换、纸墨色实际变化、持久化、ARIA 与移动端入口。

### Cloudflare Pages

线上 smoke 继续等待：

`https://wendaozhi.pages.dev/build-info.json`

追上当前 GitHub SHA 后再检查真实站点。

V2.6 新增线上断言：

- 自然察息实践卡必须出现“记录本次实践”；
- 链接必须包含 `practice=natural-breath`；
- 链接必须指向 `practice-journal` 锚点。

最新 V2.6 核心提交 `60340c73` 已通过完整链：图谱、实践质量门、31 个测试、三条 Chromium、构建产物、Cloudflare 当前提交和线上冒烟均成功。

---

## 仍保持的安全边界

八张基础实践卡继续：

```text
status: review
risk_level: S1
activity_class: B
historical_equivalence: false
```

工程测试通过只证明产品行为可重复，不证明某种传统修炼方法的功效。

S2—S4 继续不自动开放：主动调息、闭气、胎息、强意守、长时禁动、服气、内丹火候/周天、辟谷、丹药等。

---

## 下一步顺序

### P0：真实 7 天 / 30 天使用

现在不应继续堆功能。重点收集：

- 每日记录到底花多久；
- 哪些字段长期有信息价值；
- 哪些字段几乎总是默认值；
- “今日修持”是否真的减少决策负担；
- 实践卡 ↔ 工作台双向切换是否自然；
- 7天/30天复盘是否信息过载；
- “阶段方向”是否仍可能被误读为境界等级。

### P0：真机 / 多浏览器

继续验证：

- Edge；
- Safari；
- iOS Safari；
- Android Chrome；
- 手机真实 JSON 下载/导入；
- 夜读实际视觉；
- 屏幕阅读器与键盘焦点。

### P1：长期使用后的减法

只有真实使用数据支持时再考虑：

- 默认折叠哪些复盘区域；
- 删除低价值字段；
- 增加“只看异常变化”；
- 压缩30天能力卡信息密度。

### P2：深化路线研究

静修深化 / 导引深化 / 生活养修 / 丹道研究继续先研究后开放；丹道保持 `research_only`。

---

## 暂不启动

- 云端个人记录；
- 自动账号体系；
- 远程 AI 自动读取日志；
- 自动医学分诊；
- 自动严重程度判定；
- 自动晋级或境界判断；
- 无实际字段需求的 schema v2；
- S2—S4 自学教程。
